(function () {
  'use strict';

  angular.module('appModule')
    .controller('dashboardCtrl', ['$rootScope','$compile', '$timeout', 'mainCircle',
      function ($rootScope, $compile, $timeout, mainCircle) {

        // -------------- init() --------------
        let vm = this;
          
        // ------------- end init() ------------

      }
    ]);

})();